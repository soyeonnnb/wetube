extends ../base
include ../mixins/video
block content 
    form(method="GET")
        input(type="text", placeHolder="Searching by", name="keyword")
        input(type="submit", value="Search")

    div
        each video in videos 
            +video(video)