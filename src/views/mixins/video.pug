mixin video(info)
    div 
        a(href=`/videos/${info.id}`)=info.title
        p=info.description 
        small #{info.createdAt.getFullYear()}.#{info.createdAt.getMonth()+1}.#{info.createdAt.getDate()}
        hr